---
interface Props { min?: string }
const { min = '17rem' } = Astro.props;
---
<div class="grid" style={`--min:${min}`}><slot /></div>

<style>
  .grid {
    display: grid;
    gap: 1.15rem;
    grid-template-columns: repeat(auto-fill, minmax(var(--min), 1fr));
  }
</style>
